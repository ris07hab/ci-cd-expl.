name: Simple CI Pipeline

on:
  push:
    branches: [ "master" ]

jobs:
  run-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Python Script
        run: python app.py
